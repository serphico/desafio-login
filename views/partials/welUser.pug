div
    form(class="row g-3" action="/api/productos-test/", method="post") 
        div(class="col-auto")
            -var saludo = "Bienvenido "+ user 
            h2= saludo
        div(class="col-auto")
            button(type="submit" class="btn btn-primary mb-3") Logout

